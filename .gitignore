# Ignore everything by default
*

# But keep directories so git can traverse them
!*/

# Keep docker-compose files anywhere
!**/docker-compose.yml
!**/docker-compose.yaml

# (optional) also keep a root README / gitignore itself
!.gitignore
!README.md

# --- Docker runtime data (never commit) ---
**/data/
**/mysql/
**/letsencrypt/
**/postgres/
**/redis/
**/cache/
**/logs/
**/acme/
**/certs/

# --- Docker runtime data (do not commit) ---
portainer/data/
nginx/mysql/
nginx/letsencrypt/
