# Ignore everything by default
*

# But keep directories so git can traverse them
!*/

# Keep docker-compose files anywhere
!**/docker-compose.yml
!**/docker-compose.yaml

# (optional) also keep a root README / gitignore itself
!.gitignore
!.gitattributes
!README.md
!PROMPT.md

# Keep the root .env file (will be encrypted by git-crypt)
!/.env

# Keep the .env.example template (plaintext, not encrypted)
!.env.example

# But ignore local .env overrides (not encrypted)
.env.local

# Keep documentation in .ralph/agent/
!.ralph/
!.ralph/agent/
!.ralph/agent/*.md

